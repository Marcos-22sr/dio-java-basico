package dao;

import model.Board;

import java.util.Scanner;

public class CardDAO {
    public void createCard(Scanner scanner, Board board) {
        // Criar novo card no banco
    }

    public void moveCard(Scanner scanner, Board board) {
        // Lógica de movimentar card para próxima coluna
    }

    public void cancelCard(Scanner scanner, Board board) {
        // Lógica de cancelar um card
    }

    public void blockCard(Scanner scanner, Board board) {
        // Lógica de bloqueio com justificativa
    }

    public void unblockCard(Scanner scanner, Board board) {
        // Lógica de desbloqueio com justificativa
    }

    public void generateReports(Board board) {
        // Relatórios de tempo e bloqueios
    }
}
